# Hand off

Read with `flow-generate/SKILL.md`. Procedure lives here.

## Hand off
If the flow integrates with a broader feature, return to `/sf-work` for the next implementation step. If the work surfaced a non-obvious pattern (e.g., a recursion-prevention trick, a wait-element gotcha), run `/sf-compound`.

***

